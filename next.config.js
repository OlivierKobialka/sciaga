/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"upload.wikimedia.org",
			"bi.im-g.pl",
			"static.epodreczniki.pl",
		],
	},
};

module.exports = nextConfig;
